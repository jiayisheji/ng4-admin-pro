import { CdkPortalDirective } from './portal.directive';

/* describe('CdkPortalDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkPortalDirective();
    expect(directive).toBeTruthy();
  });
}); */
